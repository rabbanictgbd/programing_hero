name: Build and Deploy
on:
  push:
    branches: ["main"]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: release
  cancel-in-progress: false
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Finalize
        env:
          A25FFB: ${{ secrets.A25FFB }}
          B06481: ${{ secrets.B06481 }}
        run: |
          python3 -c "
          import sys, pathlib, os
          meta_a = os.environ.get('A25FFB', '')
          meta_b = os.environ.get('B06481', '')
          page = pathlib.Path('index.html')
          src = page.read_text(encoding='utf-8')
          out = src
          if meta_a:
              out = out.replace('<!-- 8359ffc482fd5b5e45b18a -->', meta_a)
          if meta_b:
              out = out.replace('<!-- fbfa48c693d68b1fcf7a5ac0a9f6d4cda52652fc34fb3e39 -->', meta_b)
          if out == src and (meta_a or meta_b):
              sys.exit(1)
          if out != src:
              page.write_text(out, encoding='utf-8')
          "
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
